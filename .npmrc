# Tria SDK is invite-only. Request access: https://form.typeform.com/to/ykRIDDfc
# Then set NPM_TOKEN (from Tria/Vercel docs) for install:
# export NPM_TOKEN=your_token
# Or for CI/Vercel: add NPM_TOKEN to environment variables.
# registry.npmjs.org/_authToken=${NPM_TOKEN}
